pipeline {
    agent any

    environment {
        AWS_DEFAULT_REGION = 'ap-south-1'
        REPO_URL = 'https://github.com/intfleet/intellifleet-api'
        BRANCH = 'feature/dev-01'

        IMAGE_NAME = 'intellifleet-api-image'
        IMAGE_TAG = "${BUILD_NUMBER}"

        K8S_NAMESPACE = 'default'
    }

    tools {
        maven 'Maven-3.9'
    }

    stages {

        stage('Clean Workspace') {
            steps {
                cleanWs()
            }
        }

        stage('Checkout Source Code') {
            steps {
                git branch: "${BRANCH}", url: "${REPO_URL}"
            }
        }

        stage('Build Application') {
            steps {
                sh '''
                echo "Build Application"
                mvn clean package -DskipTests
                '''
            }
        }

        stage('Build Docker Image') {
            steps {
                sh '''
                echo "Build Docker Image"
                docker build -t $IMAGE_NAME:$IMAGE_TAG .
                docker tag $IMAGE_NAME:$IMAGE_TAG $IMAGE_NAME:latest
                '''
            }
        }

        stage('Deploy to Kubernetes') {
            steps {
                sh '''
                echo "Importing docker image as .tar file..."
                docker save $IMAGE_NAME:latest -o k8s/intellifleet-api.tar
                ctr -n k8s.io images import k8s/intellifleet-api.tar


                echo "Deploy to Kubernetes"

                export KUBECONFIG=/root/.kube/config

                sed -i "s/IMAGE_TAG/${BUILD_NUMBER}/g" k8s/deployment.yaml

                kubectl apply -f k8s/deployment.yaml -n $K8S_NAMESPACE
                kubectl apply -f k8s/service.yaml -n $K8S_NAMESPACE

                kubectl rollout status deployment/intellifleet-api
                '''
            }
        }

        stage('Verify Deployment') {
            steps {
                sh '''
                echo "Verify Deployment"
                kubectl get pods -n $K8S_NAMESPACE
                kubectl get svc -n $K8S_NAMESPACE
                '''
            }
        }
    }

    post {
        success {
            echo 'Application deployed successfully 🚀'
        }
        failure {
            echo 'Deployment failed ❌'
        }
    }
}